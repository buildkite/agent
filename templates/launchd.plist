<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  This is an example launchd config for loading buildbox-agent on system boot on OS X systems using launchd

  To install:

    # Download launchd config into ~/Library/LaunchAgents/
    curl -o ~/Library/LaunchAgents/io.buildbox.buildbox-agent.plist https://raw.githubusercontent.com/buildbox/buildbox-agent/master/templates/launchd.plist

    # Edit and add your details
    open ~/Library/LaunchAgents/io.buildbox.buildbox-agent.plist

    # Load on system boot, rather than user login
    sudo ln -sfv ~/Library/LaunchAgents/io.buildbox.buildbox-agent.plist /Library/LaunchDaemons/io.buildbox.buildbox-agent.plist

    # Add it to launchd
    launchctl load /Library/LaunchDaemons/io.buildbox.buildbox-agent.plist

    # Check the launchd log
    tail -f /usr/local/var/log/buildbox-agent.log

  If you need to edit the plist:

    open /Library/LaunchDaemons/io.buildbox.buildbox-agent.plist
    launchctl unload /Library/LaunchDaemons/io.buildbox.buildbox-agent.plist
    launchctl load /Library/LaunchDaemons/io.buildbox.buildbox-agent.plist
-->
<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>io.buildbox.buildbox-agent</string>

    <key>UserName</key>
    <string>your-build-user</string>

    <key>ProgramArguments</key>
    <array>
      <string>/Users/your-build-user/.buildbox/buildbox-agent</string>
      <string>start</string>
    </array>

    <key>KeepAlive</key>
    <true/>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/usr/local/var/log/buildbox-agent.log</string>

    <key>StandardErrorPath</key>
    <string>/usr/local/var/log/buildbox-agent.log</string>

    <key>EnvironmentVariables</key>
    <dict>
      <key>PATH</key>
      <string>/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin</string>

      <key>BUILDBOX_AGENT_ACCESS_TOKEN</key>
      <string>your-agent-access-token</string>
    </dict>
  </dict>
</plist>
