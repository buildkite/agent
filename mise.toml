[tools]
golangci-lint = "2.13.1"
go = "1.26.5"
ruby = "3.4.9"

[tasks.test]
description = "Run automated tests"
run = "go tool gotestsum --format pkgname --"

[tasks.lint]
description = "Run formatting and lint checks"
run = ["go tool gofumpt -extra -w .", "golangci-lint run"]
